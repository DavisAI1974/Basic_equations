# NEW SESSION KICKOFF — S17 (continues 2026-06-02 S16)

## Read order (do it the NEW way — deltas only, not the whole master)
1. `CLAUDE.md` (repo master) — START-HERE block at top first.
2. `SESSION_HANDOFF_2026-06-02_S16.md` — this thread's single-session record.
3. `BACKLOG_tests_and_probes.md` — the queue + the standing rule.
(Master is canonical through S11; S12-S16 deltas live in handoffs — folding them into
the master is backlog #12.)

## Branch + env
- Work on the branch given at session start. Prior work on
  `claude/claude-md-strategy-pwfZr`. SessionStart hook bootstraps
  numpy/scipy/sklearn/PySR/Julia. `h5py` needed for LIGO (in requirements).
- GWOSC reachable this session (fetched GW170817). opendata.cern.ch streaming
  bypass for CERN data. Zenodo reachable.

## State in one breath
4 forces + gravity, data-level. WF + SF governing laws re-derived from raw data
(Z propagator M_Z 99.5%; QCD asymptotic freedom). Gravity chirp is method-limited
(needs Q-transform/matched-filter, not naive Hilbert). Time-blind was a method
identity, not data — the time-resolved null carries structure; the merger departs a
stable substrate reference (return = return-to-noise, H-D stays a frame). Everything
measured clusters on the equal-entropy substrate; the only departures are gravity-
associated. MI-in-null coupling is DISPROVED — do not build on it.

## FIRST ACTIONS (clear backlog before new probes — Greg's standing rule)
Top of backlog, in priority order:
1. **Substrate construction-vs-nature test** (backlog #1) — is equal-marginal-entropy
   clustering forced by symmetric-channel construction or a property of nature?
   Vary channel asymmetry; INFO-038 says asymmetry moves objects off. Decisive.
2. **Construction control: gravity-LIGO vs EM-HBT flow** (backlog #2) — decisive for
   "only gravity's flow is unbound." Needs Zenodo 5113016 (.rar 20.8MB) + unrar +
   S13 HBT loader.
3. **Proper chirp recovery** (backlog #3) — Q-transform/matched-filter on GW150914 +
   GW170817 (strain in hand).
See BACKLOG_tests_and_probes.md for the full list incl. SF femtoscopy-R, scatter (H-G),
INFO-039 promotion, OD/MASTER_DISCOVERIES, CLAUDE master-merge.

## Standing constraints
Frames never grade themselves; no pre-assigned meaning; speaking posture before+after;
Rule D (incomplete-not-wrong); no tent-widening / no discarding odd outputs (they may be
the story); >=3 seeds or real-data analogue; treat literature as conjecture; no synthesis
across construction types. MI-null disproved. Markets parked.

## Hunch register (frames, NOT graded)
H-A gravity = physics dipole + flow dipole; H-B time = flow dipole expressed; H-C only
gravity touches time; H-D black hole zeroes structure to substrate; algebra (S16): only
a time term can cancel time, so gravity must CONTAIN a time term. The S16 data note
(everything clusters on the substrate; the only departures are gravity-associated) is
the current empirical contact point for these — but it stays a frame until backlog #1
separates construction from nature.
